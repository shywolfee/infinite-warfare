funcdef bool nv_form_callback(nv_form@f, dictionary@args = null);
funcdef bool nv_form_control_callback(nv_form_control@ctrl);
funcdef void nv_form_event_handler(nv_form_event@ event);
funcdef void nv_form_network_pump();
funcdef void nv_form_audio_feedback(bool activation);
// A named interface cue: "move", "item", "edge", "wrap", "activate", "check",
// "uncheck", "slider", "switch", "tab", "open", "close", "error".
funcdef void nv_form_ui_cue_handler(const string &in cue);
